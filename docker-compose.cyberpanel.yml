version: '3.8'

services:
  backend:
    build: ./backend
    container_name: music-bot-backend
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
      - CORS_ORIGIN=https://mingbo-music-player.pages.dev